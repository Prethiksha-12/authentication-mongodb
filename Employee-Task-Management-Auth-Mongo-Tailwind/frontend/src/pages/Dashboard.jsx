import auth from"../services/authService";export default function Dashboard(){const u=auth.getUser();async function logout(){await auth.logout();location.href="/login"}return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-4xl"><div className="rounded-2xl bg-slate-900 p-6 text-white flex justify-between"><div><p className="text-indigo-300">Authentication Dashboard</p><h1 className="text-3xl font-bold">Welcome, {u?.name}</h1><p>{u?.email}</p></div><button onClick={logout} className="h-fit rounded-lg bg-red-500 px-5 py-2">Logout</button></div><div className="mt-6 rounded-2xl bg-white p-6 shadow"><h2 className="text-xl font-bold">Account</h2><p className="mt-3">Name: {u?.name}</p><p>Email: {u?.email}</p><p>Role: {u?.role}</p></div></div></main>}