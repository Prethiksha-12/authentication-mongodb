import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import AuthLayout from"../components/AuthLayout";import auth from"../services/authService";export default function ResetPassword(){const nav=useNavigate();const[f,setF]=useState({token:"",newPassword:""});const[msg,setMsg]=useState("");async function submit(e){e.preventDefault();try{const r=await auth.resetPassword(f.token,f.newPassword);setMsg(r.message);setTimeout(()=>nav("/login"),1200)}catch(e){setMsg(e.response?.data?.message||"Reset failed.")}}return <AuthLayout><h2 className="text-2xl font-bold">Reset password</h2>{msg&&<p className="mt-4 rounded bg-blue-50 p-3 text-blue-700 break-all">{msg}</p>}<form onSubmit={submit} className="mt-6 space-y-4"><textarea className="w-full rounded-lg border p-3" rows="4" placeholder="Reset token" required onChange={e=>setF({...f,token:e.target.value})}/><input className="w-full rounded-lg border p-3" type="password" minLength="8" placeholder="New password" required onChange={e=>setF({...f,newPassword:e.target.value})}/><button className="w-full rounded-lg bg-indigo-600 p-3 font-semibold text-white">Reset Password</button></form><Link className="mt-5 block text-center text-indigo-600" to="/login">Back to login</Link></AuthLayout>}