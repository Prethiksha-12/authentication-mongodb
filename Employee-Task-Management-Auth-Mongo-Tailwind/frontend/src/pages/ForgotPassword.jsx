import{useState}from"react";import{Link}from"react-router-dom";import AuthLayout from"../components/AuthLayout";import auth from"../services/authService";export default function ForgotPassword(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e){e.preventDefault();const r=await auth.forgotPassword(email);setMsg(r.developmentResetToken?`${r.message} Token: ${r.developmentResetToken}`:r.message)}return <AuthLayout><h2 className="text-2xl font-bold">Forgot password</h2>{msg&&<p className="mt-4 rounded bg-green-50 p-3 text-green-700 break-all">{msg}</p>}<form onSubmit={submit} className="mt-6 space-y-4"><input className="w-full rounded-lg border p-3" type="email" placeholder="Email" required onChange={e=>setEmail(e.target.value)}/><button className="w-full rounded-lg bg-indigo-600 p-3 font-semibold text-white">Request Reset</button></form><Link className="mt-5 block text-center text-indigo-600" to="/login">Back to login</Link></AuthLayout>}