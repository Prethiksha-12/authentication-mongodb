const r=require("express").Router();const c=require("../controllers/authController");const{authenticate,authorize}=require("../middleware/authMiddleware");r.post("/register",c.register);r.post("/login",c.login);r.post("/logout",authenticate,c.logout);r.get("/me",authenticate,c.me);r.put("/change-password",authenticate,c.changePassword);r.post("/forgot-password",c.forgotPassword);r.post("/reset-password",c.resetPassword);r.get("/admin",authenticate,authorize("ADMIN"),c.admin);r.get("/manager",authenticate,authorize("ADMIN","PROJECT_MANAGER"),c.manager);module.exports=r;